# ICRA publication-readiness audit — P0–P7

Date: 2026-07-24

**Authoritative verdict: GO for the frozen P0–P7 claim set.** The executable
audit reports 39/39 checks passing.

## What was independently recomputed

- P0–P5: the complete provenance, real-data, synthetic inference,
  stopping/safety, and pinned-simulator checks preserved by the P0–P5 audit.
- P6: 116/116 internal artifact hashes; runtime, checkpoint, config and source
  locks; three shift identities; 3 methods × 20 seeds per scenario; detector
  and recovery statistics; 4,000-resample paired effect intervals; 1,053,000
  finite unique pose rows; physical event deltas; 228 timely non-serious aborts.
- P7: 116/116 internal artifact hashes; enhanced-determinism runtime and
  checkpoint locks; three map/planner identities; 540 unique episode records;
  calibration row counts; single-attempt launches; finite posteriors; all
  success/collision/time/noninferiority intervals from 4,000 seed bootstrap
  resamples; finite unique navigation traces.

## P6/P7 headline results

P6 has zero false alarms, 100% detection and full recovery in all three
scenarios, worst p95 detection/recovery delays of 4.0/7.1 trials, and worst
full-versus-frozen CI lower bound 0.04454. Recovery uses 40% of the dense
budget. All 228 envelope aborts across controls respond within 20 ms; serious
events are zero.

P7 B8 succeeds on 180/180 map-seed episodes with zero collision. The worst
B8-versus-B0 completion-time improvement CI lower bound is 38.69 s. The worst
B8/B1 mean ratio is 1.0446 and the worst ratio CI upper bound is 1.1236,
against 1.15/1.25 gates, using 40% of the dense calibration budget.

## Claim boundary

The GO covers passive real P1 evidence, synthetic P2/P3 evidence,
replay/fault P4 evidence, and pinned Isaac Lab P5–P7 evidence. P6/P7 are
controlled simulation results. The verdict does not claim sim-to-real
generalization, online real-Go2 execution of P3–P7, or completion of P8.

Reproduce the verdict with:

```bash
python -m calibagent.cli.audit_readiness --workspace . --require-ready
```
