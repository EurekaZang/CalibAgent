source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_profile_lib.sh"
profile_common
geometry_old
speed_v6
compensation_guarded
export PROFILE_ID="h4_on_guard_v6"
export PROFILE_DESCRIPTION="First complete guard state after 03:49, before later speed and geometry changes"
export PROFILE_RISK="high"
