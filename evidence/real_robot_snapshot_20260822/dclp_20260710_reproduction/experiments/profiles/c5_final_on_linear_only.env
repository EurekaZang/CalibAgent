source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_profile_lib.sh"
profile_common
geometry_final
speed_linear_only
compensation_guarded
export PROFILE_ID="c5_final_on_linear_only"
export PROFILE_DESCRIPTION="Final geometry/guard ON with linear x0.9 only; angular reduction removed"
export PROFILE_RISK="high"
