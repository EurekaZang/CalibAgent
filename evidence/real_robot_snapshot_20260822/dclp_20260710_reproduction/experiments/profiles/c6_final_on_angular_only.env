source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_profile_lib.sh"
profile_common
geometry_final
speed_v9_angular
compensation_guarded
export PROFILE_ID="c6_final_on_angular_only"
export PROFILE_DESCRIPTION="Final geometry/guard ON with angular x0.8 only; linear reduction removed"
export PROFILE_RISK="high"
