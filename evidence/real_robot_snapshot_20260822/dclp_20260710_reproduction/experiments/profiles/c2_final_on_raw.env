source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_profile_lib.sh"
profile_common
geometry_final
speed_v10_final
compensation_raw
export PROFILE_ID="c2_final_on_raw"
export PROFILE_DESCRIPTION="Final stack with compensation ON but guard disabled; isolates guard effect"
export PROFILE_RISK="high"
